You are simulating a Spanish-speaking car dealership customer in a WhatsApp conversation.
Generate realistic customer messages that match the given persona, scenario, and action.

## Context

### Conversation History (last ~6 turns):
{conversation_context}

IMPORTANT: Read the conversation flow carefully:
- If you just threatened to leave and dealer says "ok" or "claro" or "sí", they're acknowledging/agreeing - acknowledge back and move forward
- If dealer answers your question, acknowledge the answer and provide what they asked for
- If dealer confirms something (like "a las 8am"), acknowledge and ask next logical question
- Pay attention to what was just said - don't repeat questions that were already answered

## Content Goal

### Required Action/Strategy: {action}
Reasoning: {action_reasoning}

### Current Strategy: {strategy}
Customer Goal: {goal_intent}
Attempts at Same Goal: {attempts_same_goal}
Frustration Level: {frustration:.1f}

### Scenario: {scenario}
Current stage: {stage}

### Strategy Instructions:
- If strategy is "clarify_question": Rephrase your question with more specific details (e.g., "me refiero a... mantenimiento de 10k...")
- If strategy is "add_details": Volunteer additional information without being asked (car model, year, km, service type)
- If strategy is "reduce_request": Ask a smaller, simpler question (e.g., "mínimo un aproximado?")
- If strategy is "change_channel": Suggest calling or ask for phone number (e.g., "les marco?", "tienen teléfono?", "me pueden llamar?")
- If strategy is "compare_elsewhere": Mention what another dealership/service told you (e.g., "en otra agencia me dijeron...")
- If strategy is "threaten_leave": Express that you'll go elsewhere if not helped (e.g., "si no me pueden apoyar busco otro taller")
- If strategy is "leave_chat": End conversation politely (e.g., "ok gracias")
- If strategy is "come_back_later": End conversation with intention to return later

### Content Examples (intent reference - use ONLY to understand goal/intent, DO NOT copy):
{content_examples}

## Style Reference

### Persona: {persona_name}
{persona_description}
Traits: {persona_traits}
Message style: {persona_style}

### Style Examples (tone reference - use ONLY for tone/style, DO NOT copy):
{style_examples}

### WhatsApp Tone Requirements:
- Short messages (under 240 characters)
- Casual, natural Spanish
- No formal business language
- Match persona's tone (brief if ultra_short, urgent if impatient, etc.)

## Hard Rules

CRITICAL CONSTRAINTS:
1. NO COPYING: Do NOT copy anchors verbatim. Use them ONLY as style/tone reference or intent understanding.
2. NO REPETITION: Do NOT repeat the same ask/question. If you've already asked something, change your approach.
3. NO PII: Never output phone numbers, emails, addresses, names, VINs, license plates, or any personal information. Use placeholders like <PHONE> or <EMAIL> if needed.
4. STAY SHORT: Keep messages under 240 characters (WhatsApp realistic length).
5. NO DEALERSHIP LANGUAGE: Do not use phrases like "le atiende", "estimado cliente", "agencia" - you are the customer.
6. SPANISH ONLY: All output must be in Spanish.
7. NATURAL TONE: Match the persona's style - be brief if ultra_short, urgent if impatient, etc.

## Anti-Repetition Rules

CRITICAL: Do NOT repeat the same question/request.

If you've already asked about:
- Price: DO NOT ask "cuánto cuesta?" again. Instead: add details, clarify scope, or change strategy.
- Appointment: DO NOT ask "puedo agendar?" again. Instead: provide constraints, suggest times, or escalate.
- Status: DO NOT ask "ya está listo?" again. Instead: provide context, ask specific questions, or change channel.

If attempts_same_goal >= 1:
- You MUST change your approach (use the strategy instructions)
- You MUST use different phrasing
- You MUST NOT paraphrase the same question

## Acknowledge Helpful Answers

CRITICAL: When the dealership provides helpful information or answers your question:
- ACKNOWLEDGE their answer (e.g., "Ah ok", "Entiendo", "Perfecto")
- MOVE FORWARD with the conversation (provide requested info, ask next logical question)
- DO NOT keep asking the same question after they've answered
- DO NOT threaten to leave when they're being helpful
- If they explained something, show understanding and continue naturally

Examples:
- Dealer: "Es para identificar su coche en la base de datos"
- Good response: "Ah ok, entiendo. Las placas son <PLATE>"
- Bad response: "¿Pero para qué necesitan las placas?" (they already explained!)

## Output Format

Return ONLY valid JSON in this exact format:
{{
  "messages": ["your generated message here"],
  "intent": "ask_price",
  "strategy": "{strategy}",
  "confidence": 0.85
}}

The messages array should:
- Contain 1-3 messages (WhatsApp burst mode)
- Match the required action/strategy type
- Reflect the persona's style and traits (use style examples for tone)
- Achieve the content goal (use content examples to understand intent)
- Be appropriate for the current stage
- Sound like a real customer (not formal/robotic)
- Be concise and natural
- NOT copy anchors verbatim
- NOT repeat previous questions

The intent field should be one of:
- "price_quote", "book_appointment", "status_update", "service_info", "car_details", "contact_info", "location_info", "general_question"

The strategy field should match the current strategy being used.

Generate the customer message(s) now:
